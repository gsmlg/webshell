# A sample Gemfile
source "https://rubygems.org"


group :app do
    gem 'rack'
    gem 'sinatra', :require => 'sinatra/base'
    gem 'sinatra-contrib'
    gem 'thin'
    gem 'sprockets'
    gem 'sprockets-helpers'
    gem 'erubis'
    gem 'browser'
    gem 'coffee-script'
    gem 'sass'
    gem 'uglifier'
end

group :production do

end

group :development do
    gem 'better_errors'
end
